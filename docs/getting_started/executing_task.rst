Executing Tasks
===============

When we load a dataset, you can monitor progress, task details, and historical tasks in the ``Task`` panel on the left side of the control panel.

.. image:: /fig/TaskManager.png
   :alt: Task Manager
