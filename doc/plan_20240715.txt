我们需要优先将这些功能添加进去，必须在两周内完成：
- 使用近轴明场像的旋转校正。暂时不自动配准。
- 使用深度学习的对中。也需要完善一下目前的版本，即完善使用。
- 衬度传递函数计算类 
- 适配 dm4 文件
- 打包为 Windows, MacOS, Linux 版本
- 相应的教程

近期目标：
- 在底部状态栏中添加一个进度条，和 Task Manager 同步
- 自动分析读取的数据集，判断其圆盘范围，然后计算暗场像作为预览图象
- 在读取时自动计算其径向分布密度，引入 .pvi 作为径向分布
- 添加按区域求衍射图像和的功能
- 添加叠层衍射 (增强循环求解) 的功能，提供更新重构图像的方法
- 添加椭圆畸变校正的功能
- 添加线的显示页面
- 添加线分析按钮
- 添加图像的按钮，把亮度调节的那些占空间的东西都放进去，免得它们占据主要空间
- 统一所有图像调整的按钮，都放在顶部导航栏里，重做导航栏
- 旋转校正，支持旋转后填充或裁剪零区域
- 建立专门的文档和教程网站
- 将所有调用库的函数，中心设为 ((M-1)/2, (N-1)/2)

远期目标：
- 添加使用深度学习自动进行近轴明场像的旋转校正功能
- 添加应力应变映射的功能
- 添加取向映射的功能
- 添加波动电子显微的功能
- 插件系统与标准
- 差分映射的叠层衍射重构方法
- 优化的叠层衍射重构方法
- 深度学习的叠层衍射重构方法
- 调用大模型接口实现自动编程的智能体


