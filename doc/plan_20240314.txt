目前，已经实现了4D-STEM的参数及其用途、限制等方面的映射。

此外，也已经实现了直接修改参数的页面，以及选取一般参数的页面（基于pathlike的树结构）。需要给选取一般参数的页面，添加一个右键按钮，来选定参数并给出其值的修改页面。

之后，需要编写一个页面来展示这些参数。至少，需要编写 4D-STEM 页面来展示这些参数。并且，对于那些存在于schema内的参数，需要给出按钮来直接修改。对于那些存在于schema外的参数，则通过一般的页面进行修改。

关于 img 和 vec 的页面则往后放。

接下来，需要给出 .4dstem、.img 以及 .vec 的，关于每个像素格子长度及其单位的绘制。需要实现一个绘制按钮，以及一个绘制页面，能够控制 scale bar 的长度、颜色、位置。也支持在坐标轴上进行显示。

并行计算的，往后放。

关于 vec 长度的定量化，往后放。

关于 doc，需要提前。在回稿完成后需要优先撰写 doc。

关于测试，往后放。

关于dp的位置，添加一种简单的算法来自动实现。我们只考虑两种情况，一种是稀疏的，一种是稠密的。这个需要写在回稿里，并在回稿发出去之后第一时间实现。

关于测试的